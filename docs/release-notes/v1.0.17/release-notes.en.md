# CodexPoolManager v1.0.17

Release date: 2026-08-06

## Fixes

- Corrected Codex usage-window detection when the API places a weekly-only limit in `primary_window`.
- Account cards now show only windows returned by the API and clear stale usage data after sync.
- Reopening the dashboard from the menu bar focuses the existing window instead of opening duplicates.
