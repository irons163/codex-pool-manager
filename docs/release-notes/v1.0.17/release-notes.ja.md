# CodexPoolManager v1.0.17

リリース日：2026-08-06

## 修正

- Codex API が週次のみの制限を `primary_window` に返す場合の使用量判定を修正しました。
- アカウントカードは API が返したウィンドウだけを表示し、同期時に古い使用量データを消去します。
- メニューバーからダッシュボードを開くと、重複ウィンドウを作らず既存のウィンドウを前面に表示します。
