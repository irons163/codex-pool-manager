# CodexPoolManager v1.0.17-rc.1

发布日期：2026-08-06

## 修复

- 修正 Codex API 将仅有周额度放在 `primary_window` 时的用量判断。
- 账号卡片只显示 API 实际返回的用量窗口，并在同步后清除过期数据。
- 从菜单栏重新打开主界面时，会聚焦现有窗口，不再重复打开。

## Prerelease 说明

- 此 prerelease 用于验证用量同步与单一主界面窗口行为，再进入正式版发布。
